---
import { Button } from '../../ui/Button';
import { ArrowDown } from 'lucide-react';
---

<section class="relative overflow-hidden bg-slate-50 pt-32 pb-20 lg:pt-48 lg:pb-24">
  <div class="container mx-auto px-4">
    <div class="grid lg:grid-cols-2 gap-12 lg:gap-20 items-center">
      
      <!-- Left Column: Content -->
      <div class="max-w-2xl relative z-20">
        
        <div class="inline-flex items-center rounded-full border border-sky-100 bg-sky-50 px-4 py-1.5 text-xs font-semibold tracking-wider text-sky-700 mb-8 uppercase shadow-sm">
          The Lamanify Methodology
        </div>

        <h1 class="text-5xl font-extrabold tracking-tight text-slate-900 lg:text-7xl mb-6 leading-[1.1]">
          We Don't Just<br class="hidden lg:block"> "Run Ads."<br>
          <span class="text-transparent bg-clip-text bg-gradient-to-r from-brand to-pink-600">We Build Patient Acquisition Engines.</span>
        </h1>

        <p class="text-lg text-slate-600 mb-10 leading-relaxed max-w-lg">
          Most agencies throw tactics at the wall to see what sticks. We install a proven, data-driven infrastructure into your clinic—The <strong>GrowthBooster Framework™</strong>. It’s compliant, predictable, and engineered for scale.
        </p>

        <div class="flex flex-col sm:flex-row items-center gap-6">
          <Button client:load href="#how-it-works" className="w-full sm:w-auto text-lg px-8 py-4 rounded-full bg-brand hover:bg-brand-dark shadow-lg shadow-brand/25 transition-all hover:scale-105 active:scale-95 flex items-center justify-center gap-2">
            See How It Works <ArrowDown className="w-5 h-5" />
          </Button>
        </div>
      </div>

      <!-- Right Column: Visual -->
      <div class="relative flex items-center justify-center w-full">
         <div class="relative w-full aspect-square max-w-[600px] z-10 transition-transform hover:scale-[1.02] duration-500">
            <img 
              src="/images/framework-blueprint.png" 
              alt="The GrowthBooster Framework Schematic Blueprint" 
              class="w-full h-full object-contain drop-shadow-2xl"
            />
            
            <!-- Floating Elements / Glow FX -->
            <div class="absolute inset-0 bg-gradient-to-tr from-brand/20 to-transparent opacity-30 rounded-full blur-3xl -z-10 animate-pulse"></div>
         </div>
      </div>

    </div>
  </div>
  
  <!-- Background Elements -->
  <div class="absolute top-0 left-0 w-full h-full overflow-hidden -z-10 pointer-events-none">
    <div class="absolute top-[-20%] right-[-10%] w-[800px] h-[800px] bg-slate-200/50 rounded-full blur-[120px]"></div>
    <div class="absolute bottom-[-10%] left-[-5%] w-[600px] h-[600px] bg-brand/5 rounded-full blur-[120px]"></div>
    
    <!-- Grid Pattern -->
    <div class="absolute inset-0 bg-[url('/images/grid-pattern.svg')] opacity-[0.03] bg-center [mask-image:linear-gradient(180deg,white,rgba(255,255,255,0))]"></div>
  </div>
</section>
