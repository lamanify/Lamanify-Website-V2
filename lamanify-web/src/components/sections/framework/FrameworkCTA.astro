---
import { Button } from '../../ui/Button';
---

<section class="py-24 relative overflow-hidden bg-slate-900">
  <div class="absolute inset-0 bg-brand/90 mix-blend-multiply"></div>
  <div class="absolute inset-0 bg-[url('/images/grid-pattern.svg')] opacity-10"></div>
  
  <div class="container mx-auto px-4 relative z-10 text-center">
    
    <h2 class="text-4xl md:text-5xl font-extrabold text-white mb-6 tracking-tight">
      Is Your Clinic Ready for The Framework?
    </h2>
    
    <p class="text-xl text-white/90 mb-10 font-medium max-w-2xl mx-auto">
      We only install this system for clinics that are ready to handle increased volume.
    </p>

    <div class="flex flex-col sm:flex-row gap-4 justify-center">
      <Button href="/contact" className="text-lg px-10 py-5 rounded-full bg-white text-brand hover:bg-slate-100 shadow-xl shadow-black/20 hover:scale-105 transition-all font-bold">
        Apply for a Framework Audit
      </Button>
    </div>

    <p class="mt-8 text-sm text-white/60">
      Find out if your current marketing infrastructure is holding you back.
    </p>

  </div>
</section>
