---
import { X, Check } from 'lucide-react';

const oldWayPoints = [
  { text: "Random Acts of Marketing: Posting on IG one day, boosting a post the next. No strategy.", title: "Random Acts of Marketing" },
  { text: "Vanity Metrics: celebrating \"Likes\" and \"Clicks\" while the appointment book remains empty.", title: "Vanity Metrics" },
  { text: "Compliance Roulette: risking your APC/LCP license with non-compliant copy.", title: "Compliance Roulette" },
  { text: "Leaky Bucket: paying for leads that never get followed up.", title: "Leaky Bucket" }
];

const newWayPoints = [
  { text: "Engineered Strategy: Every dollar spent has a specific job (New Patient vs. Retention).", title: "Engineered Strategy" },
  { text: "Revenue Focus: We track \"Cost Per Booked Appointment\" (CPA) and ROI.", title: "Revenue Focus" },
  { text: "Compliance Shield: Every campaign is vetted against KKLIU/MOH guidelines.", title: "Compliance Shield" },
  { text: "Closed-Loop System: We automate the follow-up so no lead is left behind.", title: "Closed-Loop System" }
];
---

<section class="py-20 bg-white">
  <div class="container mx-auto px-4">
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-5xl font-bold text-slate-900 mb-4">Why Most Marketing Fails.</h2>
      <p class="text-lg text-slate-600 max-w-2xl mx-auto">Stop wasting budget on tactics that don't scale. See the difference between the typical agency approach and a true growth engine.</p>
    </div>

    <div class="grid md:grid-cols-2 gap-8 max-w-5xl mx-auto">
      
      <!-- The Old Way -->
      <div class="bg-red-50 rounded-2xl p-8 border border-red-100">
        <h3 class="text-2xl font-bold text-slate-900 mb-8 flex items-center gap-3">
          <span class="w-8 h-8 rounded-full bg-red-100 flex items-center justify-center text-red-600">
            <X className="w-5 h-5" />
          </span>
          The "Chaos" Approach
        </h3>
        <ul class="space-y-6">
          {oldWayPoints.map((point) => (
            <li class="flex items-start gap-3">
              <X className="w-5 h-5 text-red-500 mt-1 shrink-0" />
              <div>
                <strong class="text-slate-900 block mb-1">{point.title}</strong>
                <span class="text-slate-600 text-sm leading-relaxed">{point.text.replace(`${point.title}: `, '')}</span>
              </div>
            </li>
          ))}
        </ul>
      </div>

      <!-- The GrowthBooster Protocol -->
      <div class="bg-slate-900 rounded-2xl p-8 border border-slate-700 shadow-xl relative overflow-hidden group">
        <!-- Glow effect -->
        <div class="absolute top-0 right-0 w-64 h-64 bg-brand/10 rounded-full blur-3xl -translate-y-1/2 translate-x-1/2 group-hover:bg-brand/20 transition-all duration-700"></div>

        <h3 class="text-2xl font-bold text-white mb-8 flex items-center gap-3 relative z-10">
          <span class="w-8 h-8 rounded-full bg-brand/20 flex items-center justify-center text-brand border border-brand/50">
            <Check className="w-5 h-5" />
          </span>
          The GrowthBooster Protocol
        </h3>
        <div class="space-y-6 relative z-10">
          {newWayPoints.map((point) => (
            <div class="flex items-start gap-3">
              <div class="w-5 h-5 rounded-full bg-green-500/20 flex items-center justify-center text-green-400 mt-1 shrink-0">
                <Check className="w-3 h-3" />
              </div>
              <div>
                <strong class="text-white block mb-1">{point.title}</strong>
                <span class="text-slate-400 text-sm leading-relaxed">{point.text.replace(`${point.title}: `, '')}</span>
              </div>
            </div>
          ))}
        </div>
      </div>

    </div>
  </div>
</section>
